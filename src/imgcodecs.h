#ifndef ECVL_IMGCODECS_H_
#define ECVL_IMGCODECS_H_

#include "core.h"
#include "filesystem.h"

#include <opencv2/core.hpp>
#include <opencv2/imgcodecs.hpp>

namespace ecvl {

/*
Image ImRead(const filesystem::path& filename) {
    return Image(cv::imread(filename.string()));
}


bool ImWrite(const filesystem::path& filename, const Image& src, const std::vector<int> & params) {
    return cv::imwrite(); // Cast? Si può fare? 
}
*/
} // namespace ecvl
#endif // !ECVL_IMGCODECS_H_

